;; Caps to escape/control configuration for Kanata

(defsrc
  caps
)

(defvar
  tap-time 300
  hold-time 400
)

(defalias
  escctrl (tap-hold $tap-time $hold-time esc lmet)
)

(deflayer base
  @escctrl
)
